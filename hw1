server {
  listen 80 default_server;
  listen [::]:80 default_server;
  root {{ document_root }};
  index hw0.html;

  server_name {{ server_name }};
  
  location / {
    try_files $uri $uri/ =404;
  }
}
